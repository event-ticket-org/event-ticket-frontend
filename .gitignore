node_modules/
dist/
.DS_Store
*.local

# Generated from contracts/openapi.yaml at build time, never committed. A stale
# checked-in copy is a contract that has quietly stopped being normative.
src/api/schema.d.ts

# Local Vite cache
.vite/

# Local configuration, per machine. `VITE_PAYMENT_PROVIDER` lives here so a clone with no
# Stripe account gets the fake and a machine with one does not have to remember a flag.
.env.local
